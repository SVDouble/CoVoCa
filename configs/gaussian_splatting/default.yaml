schema: gs.gaussian_splatting.config.v1
name: default

paths:
  dataset: data/sample_aruco
  calibration_result: data/sample_aruco/calibration_result.yaml
  output_dir: outputs/gaussian_splatting/default

model:
  initial_gaussian_count: 0
  use_voxel_initialization: true

optimization:
  iterations: 0
  learning_rate: 0.0

rendering:
  image_width: 640
  image_height: 480
